import React from "react";

import { Button } from "react-bootstrap";

import "./App.scss";

export const App = () => {
  return (
    <div className="App">
      <Button>Click Me</Button>
    </div>
  );
};
